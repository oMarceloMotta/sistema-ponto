import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'thera.point',
  appName: 'Point Thera',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
